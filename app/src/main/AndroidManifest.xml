<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions nécessaires -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.EasyPortal"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">

        <!-- Activités de l'application -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.EasyPortal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.EasyPortal"/>

        <activity android:name=".RegisterActivity"
            android:exported="true"
            android:theme="@style/Theme.EasyPortal"/>

        <activity android:name=".SuccessActivity"
            android:exported="true"
            android:theme="@style/Theme.EasyPortal"/>

        <activity android:name=".AdminDashboardActivity"
            android:exported="true"
            android:theme="@style/Theme.EasyPortal"/>

        <activity android:name=".UserDashboardActivity"
            android:exported="true"
            android:theme="@style/Theme.EasyPortal"/>

        <activity android:name=".LogActivity"
            android:exported="true"
            android:theme="@style/Theme.EasyPortal"/>

        <activity android:name=".PlaquesActivity" />

        <activity android:name=".SuperAdminPlaquesActivity"
            android:exported="true"
            android:theme="@style/Theme.EasyPortal"/>

        <activity android:name=".AdminPlaquesActivity"
            android:exported="true"
            android:theme="@style/Theme.EasyPortal" />
        <activity android:name=".CameraFeedActivity"
            android:exported="true"
            android:theme="@style/Theme.EasyPortal">
        </activity>

    </application>

</manifest>
